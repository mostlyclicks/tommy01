<%= render 'refinery/admin/search_header', :url => refinery.bios_admin_bios_path %>
<div class='pagination_container'>
  <% if @bios.any? %>
    <%= render 'bios' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
